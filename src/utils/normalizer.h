#ifndef NORMALIZER_H
#define NORMALIZER_H
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

double norm_tanh(double x);
double norm_fcr(double x, double lower_bound, double upper_bound);

#endif